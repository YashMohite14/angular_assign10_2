import { CostomStyleDirective } from './costom-style.directive';

describe('CostomStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CostomStyleDirective();
    expect(directive).toBeTruthy();
  });
});
